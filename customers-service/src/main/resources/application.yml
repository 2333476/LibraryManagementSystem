spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: isaac
        url: jdbc:mysql://localhost:3306/customers_db
        username: root
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                format_sql: true
        show-sql: true
    sql:
        init:
            logging:
                enabled: true
            mode: always
            platform: mysql
